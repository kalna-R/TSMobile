<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewTimeTableActivity"></activity>
        <!-- <activity android:name=".test"></activity> -->
        <activity android:name=".PaymentActivity" />
        <activity android:name=".VerifyPasswordActivity" />
        <activity android:name=".totalAmountActivity" />
        <activity android:name=".DateTimeActivity" />
        <activity android:name=".TicketTypeActivity" />
        <activity android:name=".AddTravellersActivity" />
        <activity android:name=".TokenTypeActivity" /> <!-- <activity android:name=".PurchaseTokenActivity" /> -->
        <activity android:name=".DistanceActivity" />
        <activity android:name=".QRScannerActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" /> <!-- <intent-filter> -->
        <activity android:name=".ValidActivity" />
        <activity android:name=".InvalidActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".TimeTableActivity" />
        <activity android:name=".PassengerCountActivity" /> <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity android:name=".PurchaseTokenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>